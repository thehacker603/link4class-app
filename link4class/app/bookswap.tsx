import { View, Text } from "react-native";

export default function Groups() {
  return (
    <View style={{ flex:1, justifyContent:"center", alignItems:"center" }}>
      <Text style={{ fontSize:22 }}>bookswap Page</Text>
    </View>
  );
}
